package farmSystem.closeUp.repository.winningProduct;

import farmSystem.closeUp.domain.WinningProduct;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WinningProductRepository extends JpaRepository<WinningProduct, Long> {
}
